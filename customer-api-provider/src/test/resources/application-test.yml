spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
logging:
  level:
    org:
      springframework:
        test:
          context:
            cache: DEBUG
    com:
      samples:
        customers:
          shared:
            interceptors: DEBUG
